// Replaced at container startup by docker-entrypoint.sh
// In development this file is served as-is (empty defaults — use Settings page)
window.__GARAGE_CONFIG__ = {
  apiUrl: "",
  adminToken: "",
};
